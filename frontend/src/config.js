// Backend API base URL — ensure this matches the backend `PORT` in backend/.env (default 8000)
export const BASE_URL = "http://localhost:8000/api/v1";
export const token = localStorage.getItem("token");
